---
import { Image } from 'astro:assets'
import { profile } from '../../settings'
import SocialIcons from './SocialIcons.astro'
import ProfilePictures from '@/assets/profile.png'
import { template } from '@/settings'

const { fullName } = profile
---

<aside class='sidebar-inner bg-base-200 text-base-content'>

  <div class='sidebar-content'>

    <!-- ========================================================= -->
    <!-- PROFILE                                                    -->
    <!-- ========================================================= -->

    <div>

      <div class='profile-section'>

        <Image
          class='profile-image'
          src={ProfilePictures}
          alt={`Profile picture of ${fullName}`}
        />

        <div class='profile-details'>

          <div class='profile-name'>
            {fullName}
          </div>

          <div class='profile-role'>
            Associate Scientific Director
          </div>

          <div class='profile-institute'>
            BBRC, Syngene
          </div>

        </div>

      </div>


      <!-- ======================================================= -->
      <!-- NAVIGATION                                               -->
      <!-- ======================================================= -->

      <nav class='navigation'>

        <ul>

          <li>
            <a href={`${template.base}/`}>
              Home
            </a>
          </li>

          <li>
            <a href={`${template.base}/research`}>
              Research
            </a>
          </li>

          <li>
            <a href={`${template.base}/papers`}>
              Publications
            </a>
          </li>

          <li>
            <a href={`${template.base}/patents`}>
              Patents
            </a>
          </li>

          <li>
            <a href={`${template.base}/talks`}>
              Invited Talks
            </a>
          </li>

          <li>
            <a href={`${template.base}/cv`}>
              CV
            </a>
          </li>

        </ul>

      </nav>

    </div>


    <!-- ========================================================= -->
    <!-- SOCIAL LINKS                                               -->
    <!-- ========================================================= -->

    <div class='social-links'>

      <SocialIcons />

    </div>

  </div>

</aside>


<style>
  /* ============================================================= */
  /* SIDEBAR                                                        */
  /* ============================================================= */

  .sidebar-inner {
    width: 100%;
    min-height: 100vh;
    box-sizing: border-box;
  }


  /* ============================================================= */
  /* SIDEBAR CONTENT                                                 */
  /* ============================================================= */

  .sidebar-content {
    width: 100%;
    min-height: 100vh;

    box-sizing: border-box;

    padding: 2rem 1rem;

    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }


  /* ============================================================= */
  /* PROFILE                                                         */
  /* ============================================================= */

  .profile-section {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
  }


  .profile-image {
    width: 11rem;
    height: 11rem;

    object-fit: cover;
    object-position: 50% 25%;

    border-radius: 9999px;

    flex-shrink: 0;
  }


  .profile-details {
    margin-top: 2rem;

    text-align: center;

    padding: 0 0.5rem;
  }


  .profile-name {
    font-size: 1.125rem;
    line-height: 1.25;

    font-weight: 900;
  }


  .profile-role {
    margin-top: 0.5rem;

    font-size: 0.875rem;
    line-height: 1.25;

    font-weight: 500;

    opacity: 0.6;
  }


  .profile-institute {
    margin-top: 0.25rem;

    font-size: 0.875rem;
    line-height: 1.25;

    opacity: 0.5;
  }


  /* ============================================================= */
  /* NAVIGATION                                                      */
  /* ============================================================= */

  .navigation {
    margin: 4rem 1rem 0;
  }


  .navigation ul {
    margin: 0;
    padding: 0;

    list-style: none;

    display: flex;
    flex-direction: column;

    gap: 0.5rem;
  }


  .navigation li {
    margin: 0;
    padding: 0;
  }


  .navigation a {
    display: block;

    width: 100%;

    box-sizing: border-box;

    padding: 0.55rem 0.75rem;

    color: inherit;

    font-size: 1.125rem;
    line-height: 1.5;

    text-decoration: none;

    border-radius: 0.5rem;

    transition:
      background-color 0.15s ease,
      color 0.15s ease,
      transform 0.1s ease;
  }


  /* ============================================================= */
  /* HOVER                                                          */
  /* ============================================================= */

  .navigation a:hover {
    background-color: hsl(var(--b3));
  }


  /* ============================================================= */
  /* CLICK / PRESS FEEDBACK                                         */
  /* ============================================================= */

  .navigation a:active {
    background-color: hsl(var(--b3));
    transform: scale(0.985);
  }


  /* ============================================================= */
  /* KEYBOARD FOCUS                                                 */
  /* ============================================================= */

  .navigation a:focus-visible {
    outline: 2px solid hsl(var(--p));
    outline-offset: 2px;

    background-color: hsl(var(--b3));
  }


  /* ============================================================= */
  /* SOCIAL LINKS                                                    */
  /* ============================================================= */

  .social-links {
    margin: 0 1rem;
  }


  /* ============================================================= */
  /* MOBILE                                                         */
  /* ============================================================= */

  @media (max-width: 1023px) {

    .sidebar-inner {
      width: 100%;
      min-height: 100vh;
      height: 100%;
    }


    .sidebar-content {
      width: 100%;
      min-height: 100vh;
      height: 100%;

      padding-top: 2rem;
      padding-bottom: 2rem;
    }


    .profile-image {
      width: 10rem;
      height: 10rem;
    }


    .navigation {
      margin-top: 3rem;
    }


    .navigation a {
      padding: 0.65rem 0.75rem;
    }

  }
</style>
